# Getting Started

1. Install [Node.js](https://nodejs.org/) (v18 or newer)
2. Install [Expo Go](https://expo.dev/client) on your phone
3. Clone the repo and navigate into the folder
4. Run `npm install`
5. Run `npx expo start --clear`

Scan the QR code with Expo Go (Android) or your Camera app (iOS).